const { verifyGoogleToken, getCurrentUser, updateProfile, authenticateToken } = require('./authController');

module.exports = {
  verifyGoogleToken,
  getCurrentUser,
  updateProfile,
  authenticateToken,
};
